# Edge-Optimized Agent Requirements
# Minimal dependencies for Pi3/Pi5/Laptop deployment
# Total package size: ~200MB (including models ~4-5GB additional)

# Core Agent & LLM
anthropic>=0.7.0              # Claude API client (optional, for cloud fallback)

# Database & Data
google-cloud-bigquery>=3.13.0 # BigQuery connector (optional, for data sync)
google-auth-oauthlib>=1.1.0  # GCP auth (optional)
pandas>=2.0.0                # Data manipulation
pyarrow>=13.0.0              # Parquet support

# Web Access
requests>=2.31.0             # HTTP API calls

# System Monitoring  
psutil>=5.9.0                # System info for device detection

# Embeddings & Local Search (optional, for semantic search)
onnx>=1.14.0                 # ONNX runtime (optional)
onnxruntime>=1.16.0          # ONNX execution (optional)

# Local LLM Inference (install ONE)
# Option 1: Ollama (recommended, simplest)
# ollama>=0.1.0  # Uncomment if using Ollama

# Option 2: llama.cpp Python bindings
# llama-cpp-python>=0.2.0  # Uncomment if using llamacpp

# Option 3: Hugging Face transformers (heavyweight, not recommended for Pi3)
# transformers>=4.30.0  # Not recommended for edge

# Fine-tuning Support (optional, install on training machine only)
# peft>=0.4.0           # LoRA fine-tuning framework
# torch>=2.0.0          # PyTorch (heavy, train on laptop only)
# transformers>=4.30.0  # Hugging Face models

# Utilities
python-dotenv>=1.0.0        # Environment variables
pydantic>=2.0.0             # Data validation

# Development Only (testing)
# pytest>=7.4.0              # Unit tests
# black>=23.0.0              # Code formatting
