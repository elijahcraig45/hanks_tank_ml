# Pinned deliberately: the Cloud Function retrains weekly and both training and inference
# load/write .pkl artifacts. Unpinned resolvers let a redeploy pick up a new
# scikit-learn/XGBoost and either break pickle loads or silently shift predictions.
# Versions below match the local .venv that produced the current V10 artifact.
# Bump these intentionally, then verify the CF can still load models/*.pkl.
# Mirrored byte-for-byte to src/requirements.txt (the copy the CF deploys) — change both.

# Core GCP
google-cloud-bigquery==3.39.0
google-cloud-storage==3.7.0
google-cloud-aiplatform==1.132.0
# Not installed locally — could not verify a working version, left as a floor.
google-cloud-bigquery-storage>=2.20.0
functions-framework>=3.0.0

# Data & ML — pickle-compatibility critical, keep exact
pandas==2.3.3
numpy==2.4.0
scikit-learn==1.8.0
# Pinned explicitly rather than leaning on scikit-learn to pull it in: the
# Bradley-Terry power rankings build a sparse design matrix with scipy.sparse,
# so it is a direct dependency now and an unpinned transitive one could shift.
scipy==1.16.3
xgboost==3.1.3
lightgbm==4.6.0
catboost==1.2.10
optuna==4.7.0

# Data ingestion
requests==2.32.5
MLB-StatsAPI==1.9.0
pybaseball==2.2.7

# Serialization & utilities
pyarrow==22.0.0
db-dtypes==1.5.0
pytz==2025.2
# Not installed locally — could not verify. ephem powers the moon_phase V7+ feature.
ephem>=4.1
python-dotenv>=1.0.0
